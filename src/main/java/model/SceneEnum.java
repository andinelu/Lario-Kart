/**
 * This class contains the game's enum's
 */
public enum SceneEnum {
    STARTUP,
    LOGIN,
    REGISTER,
    MAINMENU,
    PLAY,
    JOIN,
    CREATE,
    LOBBY,
    GAME,
    STATISTICS,
    HIGHSCORE
}
